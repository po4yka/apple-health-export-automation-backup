apiVersion: 1

providers:
  - name: 'Health Dashboards'
    orgId: 1
    folder: 'Apple Health'
    folderUid: 'apple-health'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
